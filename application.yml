eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

address:
  provider:
    url: http://address-service/address/
    
user:
  message: Hello pradip this is git 


spring:
  datasource:
    name: springboot
    url: jdbc:mysql:// localhost:3306/springboot
    username: root
    password: 99097@Pradip
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
